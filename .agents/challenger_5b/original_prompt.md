Your task is to empirically challenge and test the fixes made in `scripts/update.sh` and `scripts/setup_pi.sh`.

You MUST:
1. Write test scripts to simulate the LPE attack: can the `pi` user modify the updater executed by systemd? Check file ownership and permissions for `/usr/local/bin/trainingkiosk-update` generated by `setup_pi.sh`.
2. Write test scripts to test the trap logic: Does a failing command (`set -e`) trigger the reboot trap exactly once? Does `SIGTERM` trigger it exactly once? Do they exit correctly without looping?
Note: the actual script will reboot the machine (or try to). You may need to mock `/sbin/reboot` during your tests, or just execute the script in a test environment.

Write and run these empirical tests. If they pass and the vulnerabilities are addressed, VERDICT=PASS. If not, VERDICT=FAIL.
Write a report to `handoff.md` and send a message with the file path.
Workspace: C:/Users/sirho/Desktop/Kiosk v2/trainingkiosk/.agents/challenger_5b
